name: CI - Stashi Wallet

on:
  push:
    branches: [main, develop]
    tags:
      - "v*"
  pull_request:
    branches: [main, develop]

env:
  RUST_VERSION: "1.90.0"
  FLUTTER_VERSION: "3.47.2"
  DART_VERSION: "3.13.2"
  FRB_CODEGEN_VERSION: "2.11.1"
  GO_VERSION: "1.21.13"
  JAVA_VERSION: "25"
  NODE_VERSION: "24"
  NPM_VERSION: "12.0.2"
  COCOAPODS_VERSION: "1.17.0"
  APPIMAGETOOL_URL: "https://github.com/AppImage/appimagetool/releases/download/1.9.1/appimagetool-x86_64.AppImage"
  APPIMAGETOOL_SHA256: "ed4ce84f0d9caff66f50bcca6ff6f35aae54ce8135408b3fa33abfc3cb384eb0"
  APPIMAGE_RUNTIME_URL: "https://github.com/AppImage/type2-runtime/releases/download/20251108/runtime-x86_64"
  APPIMAGE_RUNTIME_SHA256: "2fca8b443c92510f1483a883f60061ad09b46b978b2631c807cd873a47ec260d"
  ANDROID_BUILD_TOOLS_VERSION: "36.0.0"
  ANDROID_PLATFORM_VERSION: "36"
  # Match Flutter 3.47.2; NDK r26 cannot assemble OpenSSL 3.6's x86 SM3 routines.
  ANDROID_NDK_VERSION: "28.2.13676358"
  SYFT_VERSION: "1.40.1"
  SYFT_SHA256_LINUX_AMD64: "c229137c919f22aa926c1c015388db5ec64e99c078e0baac053808e8f36e2e00"
  SYFT_SHA256_DARWIN_ARM64: "c0f6a4fc0563ef1dfe1acf9a4518db66cb37bbb1391889aba3be773dff3487dd"
  SYFT_SHA256_DARWIN_AMD64: "9e84d1f152ef9d3bb541cc7cedf81ed4c7ed78f6cc2e4c8f0db9e052b64cd7be"
  SYFT_SHA256_WINDOWS_AMD64: "eedac363e277dfecac420b6e4ed0a861bc2c9c84a7544157f52807a99bff07cd"
  CARGO_AUDIT_VERSION: "0.22.0"
  CARGO_DENY_VERSION: "0.19.0"
  CARGO_AUDITABLE_VERSION: "0.7.2"
  CARGO_TERM_COLOR: always
  GITHUB_API_PUBLIC_READONLY_TOKEN: ${{ github.token }}

jobs:
  workflow-action-pinning:
    name: Security - Pinned GitHub Actions
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Verify immutable GitHub Action refs
        run: python3 scripts/check_pinned_actions.py

      - name: Test build and asset policies
        run: python3 -m unittest discover -s scripts/tests -p 'test_*.py' -v

  frb-codegen:
    name: FRB - Codegen
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Cache cargo
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/bin
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-frb-${{ hashFiles('**/Cargo.lock') }}

      - name: Install flutter_rust_bridge_codegen
        run: cargo install flutter_rust_bridge_codegen --locked --version ${{ env.FRB_CODEGEN_VERSION }} --force

      - name: Prepare Flutter Dart toolchain
        run: |
          cd app
          flutter pub get --enforce-lockfile
          cd ..
          mkdir -p .dart_tool
          cp app/.dart_tool/package_config.json .dart_tool/package_config.json

      - name: Generate flutter_rust_bridge bindings
        run: |
          FRB_SIMPLE_BUILD_SKIP=1 flutter_rust_bridge_codegen generate --config-file flutter_rust_bridge.yaml

      - name: Verify bindings are up to date
        run: git diff --exit-code

  # ========================================================================
  # Rust Quality Gates
  # ========================================================================

  rust-lint:
    name: Rust - Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}
          components: rustfmt, clippy

      - name: Cache cargo registry
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache cargo build
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: crates/target
          key: ${{ runner.os }}-cargo-build-${{ hashFiles('**/Cargo.lock') }}

      - name: Check formatting
        run: |
          cd crates
          cargo fmt --all -- --check

      - name: Run clippy
        run: |
          cd crates
          cargo clippy --all-targets --all-features --locked -- -D warnings

  rust-test:
    name: Rust - Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Cache cargo
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            crates/target
          key: ${{ runner.os }}-cargo-test-${{ hashFiles('**/Cargo.lock') }}

      - name: Run unit tests
        run: |
          cd crates
          cargo test --all-features --locked

  rust-qortal-tests:
    name: Rust - Qortal Adapter Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Set up Java
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c
        with:
          distribution: temurin
          java-version: "17"

      - name: Cache cargo
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            crates/target
          key: ${{ runner.os }}-cargo-qortal-${{ hashFiles('**/Cargo.lock') }}

      - name: Run Qortal-focused checks
        run: |
          cd crates
          cargo test -p pirate-cli-core --lib qortal_ --locked
          cargo check -p pirate-wallet-service --locked
          cargo test -p pirate-core qortal_p2sh --locked -- --nocapture
          cargo test -p pirate-qortal-jni --locked
          cargo build --release -p pirate-qortal-cli --locked
          cargo build --release -p pirate-qortal-jni --locked
          mkdir -p target/qortal-jni-java
          javac -d target/qortal-jni-java \
            ../bindings/qortal-jni/src/main/java/com/rust/litewalletjni/LiteWalletJni.java \
            ../bindings/qortal-jni/src/test/java/com/rust/litewalletjni/QortalJniSmoke.java
          java \
            -Dqortal.jni.library="$(pwd)/target/release/librust.so" \
            -Dqortal.jni.storage="$(pwd)/target/qortal-jni-smoke-wallet" \
            -cp target/qortal-jni-java \
            com.rust.litewalletjni.QortalJniSmoke

  rust-property-tests:
    name: Rust - Property Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Cache cargo
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            crates/target
          key: ${{ runner.os }}-cargo-proptest-${{ hashFiles('**/Cargo.lock') }}

      - name: Run property tests
        run: |
          cd crates/pirate-core
          cargo test --test property_tests --release --locked

  rust-migration-tests:
    name: Rust - Migration Snapshot Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Cache cargo
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            crates/target
          key: ${{ runner.os }}-cargo-migration-${{ hashFiles('**/Cargo.lock') }}

      - name: Run migration tests
        run: |
          cd crates/pirate-storage-sqlite
          cargo test --test migration_snapshots --locked

  rust-perf-tests:
    name: Rust - Performance Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Cache cargo
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            crates/target
          key: ${{ runner.os }}-cargo-perf-${{ hashFiles('**/Cargo.lock') }}

      - name: Run performance benchmarks
        run: |
          cd crates/pirate-sync-lightd
          cargo test --test performance_benchmarks --release --locked -- --nocapture

  # ========================================================================
  # Security Audits
  # ========================================================================

  security-audit:
    name: Security - Cargo Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Install cargo-audit
        run: cargo install cargo-audit --locked --version ${{ env.CARGO_AUDIT_VERSION }}

      - name: Run cargo audit
        run: |
          cd crates
          # Transitive through Arti/Tor; no fixed rsa release exists yet.
          cargo audit --ignore RUSTSEC-2023-0071

  security-deny:
    name: Security - Cargo Deny
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Install cargo-deny
        run: cargo install cargo-deny --locked --version ${{ env.CARGO_DENY_VERSION }}

      - name: Run cargo deny
        run: |
          cd crates
          cargo deny check --config ../deny.toml

  security-semgrep-rust:
    name: Security - Semgrep (Rust)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.11"

      - name: Install Semgrep
        run: python -m pip install --upgrade pip semgrep==1.36.0

      - name: Run Semgrep on Rust files
        env:
          SEMGREP_PARALLELISM: "1"
        run: semgrep scan --config .semgrep.yml --error --metrics off --jobs 1 --max-memory 1500 --timeout 45 --include="crates/**/*.rs"

  security-semgrep-dart:
    name: Security - Semgrep (Dart)
    runs-on: ubuntu-latest
    # Disabled: Dart scans exhaust CI memory even with minimal rules
    # Security-critical code (crypto, networking, keys) is in Rust (covered by security-semgrep-rust)
    # Dart layer is primarily UI - manual code review covers UI security
    if: false
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.11"

      - name: Install Semgrep
        run: python -m pip install --upgrade pip semgrep==1.36.0

      - name: Run Semgrep on Dart files
        env:
          SEMGREP_PARALLELISM: "1"
        run: semgrep scan --config .semgrep.yml --error --metrics off --jobs 1 --max-memory 2000 --timeout 60 --include="app/lib/**/*.dart"

  # ========================================================================
  # Flutter Quality Gates
  # ========================================================================

  flutter-analyze:
    name: Flutter - Analyze
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable

      - name: Get dependencies
        run: |
          cd app
          flutter pub get --enforce-lockfile

      - name: Audit translations
        run: |
          cd app
          dart run tool/audit_runtime_translations.dart

      - name: Analyze code
        run: |
          cd app
          flutter analyze --fatal-infos

  flutter-test:
    name: Flutter - Unit & Widget Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable

      - name: Get dependencies
        run: |
          cd app
          flutter pub get --enforce-lockfile

      - name: Run tests
        run: |
          bash scripts/test-flutter.sh --coverage

      - name: Upload coverage
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f
        with:
          files: ./app/coverage/lcov.info
          flags: flutter
          fail_ci_if_error: false

  flutter-integration-tests:
    name: Flutter - Integration Tests
    runs-on: macos-latest  # Use macOS for iOS simulator
    # Skip integration tests in CI - they require a live lightwalletd backend
    if: false
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Build Rust FFI library
        run: |
          cd crates
          cargo build --release --package pirate-ffi-frb --features frb --no-default-features --locked

      - name: Get dependencies
        run: |
          cd app
          flutter pub get --enforce-lockfile

      - name: Run integration tests
        run: |
          cd app
          if ! ../scripts/e2e-preflight.sh; then
            echo "Skipping integration tests (missing E2E prerequisites)."
            exit 0
          fi
          cd ..
          bash scripts/prepare-flutter-build.sh macos
          cd app
          flutter config --enable-macos-desktop
          flutter test integration_test/ -d macos

  # ========================================================================
  # Build Matrix
  # ========================================================================

  build-matrix:
    name: Build - ${{ matrix.platform }}
    runs-on: ${{ matrix.os }}
    needs:
      - workflow-action-pinning
    env:
      GRADLE_OPTS: "-Dorg.gradle.vfs.watch=false -Dorg.gradle.internal.repository.max.retries=5 -Dorg.gradle.internal.repository.initial.backoff=500"
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: Android
            os: ubuntu-latest
            build-command: flutter build apk --release --split-per-abi --target-platform=android-arm64,android-arm
          - platform: iOS
            os: macos-latest
            build-command: ios unsigned xcodebuild
          - platform: Linux
            os: ubuntu-22.04
            build-command: flutter build linux --release
          - platform: macOS
            os: macos-latest
            build-command: flutter build macos --release
          - platform: Windows
            os: windows-2025-vs2026
            build-command: flutter build windows --release

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable

      - name: Setup Java
        if: matrix.platform == 'Android'
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Android SDK
        if: matrix.platform == 'Android'
        uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699

      - name: Setup Gradle
        if: matrix.platform == 'Android'
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb

      - name: Install CocoaPods
        if: matrix.platform == 'iOS' || matrix.platform == 'macOS'
        run: gem install cocoapods -v ${{ env.COCOAPODS_VERSION }}

      - name: Install Linux dependencies
        if: matrix.platform == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev libsecret-1-dev

      - name: Resolve tagged application version
        run: bash scripts/sync-version-from-tag.sh

      - name: Get dependencies
        run: |
          cd app
          flutter pub get --enforce-lockfile

      - name: Enable Linux desktop
        if: matrix.platform == 'Linux'
        run: flutter config --enable-linux-desktop

      - name: Enable macOS desktop
        if: matrix.platform == 'macOS'
        run: flutter config --enable-macos-desktop

      - name: Enable Windows desktop
        if: matrix.platform == 'Windows'
        run: flutter config --enable-windows-desktop

      - name: Prepare hermetic iOS assets
        if: matrix.platform == 'iOS'
        env:
          GITHUB_API_PUBLIC_READONLY_TOKEN: ${{ github.token }}
        run: |
          bash scripts/prepare-flutter-build.sh ios

      - name: Prepare hermetic Linux assets
        if: matrix.platform == 'Linux'
        env:
          GITHUB_API_PUBLIC_READONLY_TOKEN: ${{ github.token }}
        run: |
          bash scripts/prepare-flutter-build.sh linux

      - name: Prepare hermetic macOS assets
        if: matrix.platform == 'macOS'
        env:
          GITHUB_API_PUBLIC_READONLY_TOKEN: ${{ github.token }}
        run: |
          bash scripts/prepare-flutter-build.sh macos

      - name: Prepare hermetic Windows assets
        if: matrix.platform == 'Windows'
        env:
          GITHUB_API_PUBLIC_READONLY_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          bash scripts/prepare-flutter-build.sh windows

      - name: Prepare hermetic Android assets
        if: matrix.platform == 'Android'
        env:
          GITHUB_API_PUBLIC_READONLY_TOKEN: ${{ github.token }}
        run: |
          bash scripts/prepare-flutter-build.sh android

      - name: Prime Android Gradle plugin cache
        if: matrix.platform == 'Android'
        run: |
          cd app/android
          chmod +x gradlew
          status=1
          for attempt in 1 2 3; do
            if ./gradlew --no-daemon help; then
              exit 0
            else
              status=$?
            fi
            if [ "$attempt" -lt 3 ]; then
              sleep $((attempt * 20))
            fi
          done
          exit "$status"

      - name: Build Android
        if: matrix.platform == 'Android'
        run: |
          cd app
          status=1
          for attempt in 1 2 3; do
            if flutter build apk --release --split-per-abi --target-platform=android-arm64,android-arm; then
              exit 0
            else
              status=$?
            fi
            if [ "$attempt" -lt 3 ]; then
              sleep $((attempt * 20))
            fi
          done
          exit "$status"

      - name: Verify Android release shrinker output
        if: matrix.platform == 'Android'
        run: python3 scripts/verify-android-release-shrinker.py app/build/app/outputs/mapping/release/mapping.txt

      - name: Build iOS
        if: matrix.platform == 'iOS'
        run: |
          cd app
          flutter build ios --release --no-codesign --config-only
          status=1
          for attempt in 1 2 3; do
            if xcodebuild \
              -workspace ios/Runner.xcworkspace \
              -scheme Runner \
              -configuration Release \
              -sdk iphoneos \
              -destination "generic/platform=iOS" \
              CODE_SIGN_STYLE=Manual \
              CODE_SIGNING_ALLOWED=NO \
              CODE_SIGNING_REQUIRED=NO \
              CODE_SIGN_IDENTITY="" \
              DEVELOPMENT_TEAM="" \
              PROVISIONING_PROFILE_SPECIFIER="" \
              build; then
              exit 0
            else
              status=$?
            fi
            if [ "$attempt" -lt 3 ]; then
              echo "iOS build attempt $attempt failed; retrying..."
              sleep $((attempt * 20))
            fi
          done
          exit "$status"

      - name: Build Linux
        if: matrix.platform == 'Linux'
        run: |
          cd app
          status=1
          for attempt in 1 2 3; do
            if flutter build linux --release; then
              exit 0
            else
              status=$?
            fi
            if [ "$attempt" -lt 3 ]; then
              echo "Linux build attempt $attempt failed; retrying..."
              sleep $((attempt * 20))
            fi
          done
          exit "$status"

      - name: Verify Linux build compatibility
        if: matrix.platform == 'Linux'
        run: python3 scripts/verify_linux_glibc.py --max-version 2.35 app/build/linux/x64/release/bundle

      - name: Build macOS
        if: matrix.platform == 'macOS'
        env:
          FLUTTER_XCODE_CODE_SIGNING_ALLOWED: "NO"
          FLUTTER_XCODE_CODE_SIGNING_REQUIRED: "NO"
        run: |
          cd app
          status=1
          for attempt in 1 2 3; do
            if flutter build macos --release; then
              exit 0
            else
              status=$?
            fi
            if [ "$attempt" -lt 3 ]; then
              echo "macOS build attempt $attempt failed; retrying..."
              sleep $((attempt * 20))
            fi
          done
          exit "$status"

      - name: Build Windows
        if: matrix.platform == 'Windows'
        shell: bash
        run: |
          cd app
          status=1
          for attempt in 1 2 3; do
            if flutter build windows --release; then
              exit 0
            else
              status=$?
            fi
            if [ "$attempt" -lt 3 ]; then
              echo "Windows build attempt $attempt failed; retrying..."
              sleep $((attempt * 20))
            fi
          done
          exit "$status"

      - name: Build ${{ matrix.platform }}
        if: matrix.platform != 'Android' && matrix.platform != 'iOS' && matrix.platform != 'Linux' && matrix.platform != 'macOS' && matrix.platform != 'Windows'
        env:
          OVERRIDE_DEFI_API_DOWNLOAD: "false"
        run: |
          cd app
          ${{ matrix.build-command }}

  backend-release-plan:
    name: Release - Backend Artifact Diff
    runs-on: ubuntu-latest
    needs:
      - workflow-action-pinning
    if: github.event_name == 'pull_request' || (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v')))
    outputs:
      cli_changed: ${{ steps.diff.outputs.cli_changed }}
      qortal_cli_changed: ${{ steps.diff.outputs.qortal_cli_changed }}
      qortal_jni_changed: ${{ steps.diff.outputs.qortal_jni_changed }}
      native_ffi_changed: ${{ steps.diff.outputs.native_ffi_changed }}
      ios_sdk_changed: ${{ steps.diff.outputs.ios_sdk_changed }}
      android_sdk_changed: ${{ steps.diff.outputs.android_sdk_changed }}
      react_native_plugin_changed: ${{ steps.diff.outputs.react_native_plugin_changed }}
      react_native_plugin_version: ${{ steps.diff.outputs.react_native_plugin_version }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0

      - name: Detect backend artifact version changes
        id: diff
        run: |
          python3 scripts/detect-release-artifacts.py --github-output "$GITHUB_OUTPUT"
          python3 scripts/detect-release-artifacts.py

  package-cli:
    name: Package - CLI (${{ matrix.platform }})
    runs-on: ${{ matrix.os }}
    needs:
      - rust-test
      - backend-release-plan
    if: github.event_name == 'pull_request' || (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v')))
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: Linux
            os: ubuntu-22.04
            bin-ext: ""
            artifact-name: cli-linux
          - platform: macOS
            os: macos-15
            bin-ext: ""
            artifact-name: cli-macos
          - platform: Windows
            os: windows-2025-vs2026
            bin-ext: ".exe"
            artifact-name: cli-windows
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Install Perl (OpenSSL build)
        if: matrix.platform == 'Windows'
        shell: pwsh
        run: |
          choco install strawberryperl --version 5.42.0.1 --no-progress -y
          "C:\Strawberry\perl\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding ascii -Append

      - name: Build CLI binaries
        shell: bash
        env:
          CARGO_INCREMENTAL: "0"
          RUST_BACKTRACE: "1"
        run: |
          chmod +x scripts/build-cli.sh
          scripts/build-cli.sh

      - name: Collect CLI binaries
        shell: bash
        run: |
          mkdir -p dist/cli
          cp "crates/target/release/piratewallet-cli${{ matrix.bin-ext }}" dist/cli/
          cp "crates/target/release/pirate-qortal-cli${{ matrix.bin-ext }}" dist/cli/

      - name: Verify Linux CLI compatibility
        if: matrix.platform == 'Linux'
        run: python3 scripts/verify_linux_glibc.py --max-version 2.35 dist/cli

      - name: Upload CLI binaries
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: ${{ matrix.artifact-name }}
          path: dist/cli/*

  package-qortal-jni:
    name: Package - Qortal JNI (${{ matrix.platform }})
    runs-on: ${{ matrix.os }}
    needs:
      - rust-qortal-tests
      - backend-release-plan
    if: github.event_name == 'pull_request' || (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v')))
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: Linux x86_64
            os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            artifact-name: qortal-jni-linux-x86_64
          - platform: Linux aarch64
            os: ubuntu-22.04-arm
            target: aarch64-unknown-linux-gnu
            artifact-name: qortal-jni-linux-aarch64
          - platform: macOS x86_64
            os: macos-15-intel
            target: x86_64-apple-darwin
            artifact-name: qortal-jni-macos-x86_64
          - platform: macOS aarch64
            os: macos-15
            target: aarch64-apple-darwin
            artifact-name: qortal-jni-macos-aarch64
          - platform: Windows x86_64
            os: windows-2025-vs2026
            target: x86_64-pc-windows-msvc
            artifact-name: qortal-jni-windows-x86_64
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}
          targets: ${{ matrix.target }}

      - name: Install Perl (OpenSSL build)
        if: startsWith(matrix.platform, 'Windows')
        shell: pwsh
        run: |
          choco install strawberryperl --version 5.42.0.1 --no-progress -y
          "C:\Strawberry\perl\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding ascii -Append

      - name: Build Qortal JNI library
        shell: bash
        env:
          CARGO_INCREMENTAL: "0"
          QORTAL_JNI_TARGET: ${{ matrix.target }}
        run: |
          chmod +x scripts/build-qortal-jni.sh
          scripts/build-qortal-jni.sh

      - name: Verify Linux Qortal JNI compatibility
        if: startsWith(matrix.platform, 'Linux')
        run: python3 scripts/verify_linux_glibc.py --max-version 2.35 dist/qortal-jni

      - name: Upload Qortal JNI library
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: ${{ matrix.artifact-name }}
          path: dist/qortal-jni/**/*

  package-native-ffi:
    name: Package - Native FFI
    runs-on: ubuntu-22.04
    needs:
      - rust-test
      - backend-release-plan
    if: github.event_name == 'pull_request' || (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v')))
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Build native FFI
        run: |
          chmod +x scripts/build-native-ffi.sh
          ./scripts/build-native-ffi.sh

      - name: Collect native SDK inputs
        run: |
          set -euo pipefail
          mkdir -p dist/native-ffi/ios-sdk dist/native-ffi/android-sdk
          find crates/target/release -maxdepth 1 -type f \( \
            -name 'libpirate_ffi_native.a' \
            -o -name 'libpirate_ffi_native.so' \
            -o -name 'pirate_ffi_native.dll' \
            -o -name 'pirate_ffi_native.lib' \
          \) -exec cp -f {} dist/native-ffi/ \;
          test -s crates/pirate-ffi-native/pirate_wallet_service.h
          cp -f crates/pirate-ffi-native/pirate_wallet_service.h dist/native-ffi/
          cp -R bindings/ios-sdk/. dist/native-ffi/ios-sdk/
          cp -R bindings/android-sdk/. dist/native-ffi/android-sdk/

      - name: Verify Linux native FFI compatibility
        run: python3 scripts/verify_linux_glibc.py --max-version 2.35 dist/native-ffi

      - name: Upload native FFI package
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: native-ffi
          path: dist/native-ffi/**/*

  package-ios-sdk:
    name: Package - iOS SDK
    runs-on: macos-15
    needs:
      - rust-test
      - backend-release-plan
    if: github.event_name == 'pull_request' || (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v')))
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Select Xcode
        run: |
          set -euo pipefail
          if [ -d /Applications/Xcode_26.3.app ]; then
            export DEVELOPER_DIR=/Applications/Xcode_26.3.app/Contents/Developer
          elif [ -d /Applications/Xcode_26.2.app ]; then
            export DEVELOPER_DIR=/Applications/Xcode_26.2.app/Contents/Developer
          else
            echo "No supported Xcode 26.x installation found on runner" >&2
            ls -1 /Applications | grep '^Xcode' || true
            exit 1
          fi
          echo "DEVELOPER_DIR=$DEVELOPER_DIR" >> "$GITHUB_ENV"
          xcodebuild -version

      - name: Build iOS SDK
        run: |
          chmod +x scripts/build-ios-sdk.sh
          ./scripts/build-ios-sdk.sh

      - name: Verify iOS SDK Swift package
        run: |
          cd bindings/ios-sdk
          SCHEME="$(xcodebuild -list -json | python3 -c 'import json, sys; data = json.load(sys.stdin); schemes = data.get("workspace", {}).get("schemes") or data.get("project", {}).get("schemes") or []; preferred = ["PirateWalletSDK", "PirateWalletSDK-Package"]; print(next((name for name in preferred if name in schemes), schemes[0] if schemes else "")); sys.exit(0 if schemes else 1)')"
          rm -rf /tmp/pirate-ios-sdk-derived-data
          xcodebuild \
            -scheme "$SCHEME" \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath /tmp/pirate-ios-sdk-derived-data \
            build

      - name: Smoke test iOS SDK package
        run: |
          cd bindings/ios-sdk
          SCHEME="$(xcodebuild -list -json | python3 -c 'import json, sys; data = json.load(sys.stdin); schemes = data.get("workspace", {}).get("schemes") or data.get("project", {}).get("schemes") or []; preferred = ["PirateWalletSDK", "PirateWalletSDK-Package"]; print(next((name for name in preferred if name in schemes), schemes[0] if schemes else "")); sys.exit(0 if schemes else 1)')"
          DESTINATION="$(xcrun simctl list devices available -j | python3 -c 'import json, sys; devices = json.load(sys.stdin).get("devices", {}); preferred = [("com.apple.CoreSimulator.SimRuntime.iOS-26-2", "iPhone 17"), ("com.apple.CoreSimulator.SimRuntime.iOS-26-2", "iPhone 16"), ("com.apple.CoreSimulator.SimRuntime.iOS-26-1", "iPhone 17"), ("com.apple.CoreSimulator.SimRuntime.iOS-26-1", "iPhone 16"), ("com.apple.CoreSimulator.SimRuntime.iOS-18-6", "iPhone 16"), ("com.apple.CoreSimulator.SimRuntime.iOS-18-5", "iPhone 16")]; available = [(runtime, device.get("name")) for runtime, entries in devices.items() if "iOS" in runtime for device in entries if device.get("isAvailable")]; chosen = next((name for runtime_name, device_name in preferred for runtime, name in available if runtime == runtime_name and name == device_name), None); chosen = chosen or (available[0][1] if available else ""); print("platform=iOS Simulator,name=" + chosen); sys.exit(0 if chosen else 1)')"
          rm -rf /tmp/pirate-ios-sdk-tests
          xcodebuild \
            -scheme "$SCHEME" \
            -destination "$DESTINATION" \
            -derivedDataPath /tmp/pirate-ios-sdk-tests \
            test

      - name: Upload iOS SDK package
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: ios-sdk
          path: |
            dist/ios-sdk/*
            bindings/ios-sdk/Frameworks/PirateWalletNative.xcframework/**/*
            bindings/ios-sdk/Package.swift
            bindings/ios-sdk/Sources/PirateWalletSDK/*
            bindings/ios-sdk/Tests/PirateWalletSDKTests/*

  package-android-sdk:
    name: Package - Android SDK
    runs-on: ubuntu-latest
    env:
      JAVA_VERSION: "21"
      GRADLE_OPTS: "-Dorg.gradle.vfs.watch=false -Dorg.gradle.internal.repository.max.retries=5 -Dorg.gradle.internal.repository.initial.backoff=500"
    needs:
      - rust-test
      - backend-release-plan
    if: github.event_name == 'pull_request' || (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v')))
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Setup Java
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}

      - name: Setup Android SDK
        uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699

      - name: Install Android SDK packages
        run: |
          yes | sdkmanager "platforms;android-${{ env.ANDROID_PLATFORM_VERSION }}" "build-tools;${{ env.ANDROID_BUILD_TOOLS_VERSION }}" "ndk;${{ env.ANDROID_NDK_VERSION }}"
          echo "ANDROID_NDK_HOME=${ANDROID_SDK_ROOT}/ndk/${{ env.ANDROID_NDK_VERSION }}" >> $GITHUB_ENV
          echo "ANDROID_NDK_ROOT=${ANDROID_SDK_ROOT}/ndk/${{ env.ANDROID_NDK_VERSION }}" >> $GITHUB_ENV

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb
        with:
          gradle-version: "8.13"

      - name: Build Android SDK
        run: |
          chmod +x scripts/build-android-sdk.sh
          ./scripts/build-android-sdk.sh

      - name: Smoke test Android SDK consumer app
        env:
          GRADLE_USER_HOME: ${{ runner.temp }}/gradle-android-sdk-smoke
        run: |
          cd bindings/android-sdk
          status=1
          for attempt in 1 2 3; do
            if gradle --no-daemon :smoke-consumer:assembleDebug; then
              exit 0
            else
              status=$?
            fi
            if [ "$attempt" -lt 3 ]; then
              sleep $((attempt * 20))
            fi
          done
          exit "$status"

      - name: Upload Android SDK package
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: android-sdk
          path: |
            dist/android-sdk/*
            bindings/android-sdk/build/outputs/aar/*.aar
            bindings/android-sdk/src/main/jniLibs/**/*
            bindings/android-sdk/src/main/kotlin/com/pirate/wallet/sdk/*
            bindings/android-sdk/smoke-consumer/**/*
            bindings/android-sdk/build.gradle.kts
            bindings/android-sdk/settings.gradle.kts

  package-react-native-plugin:
    name: Package - React Native Plugin
    runs-on: ubuntu-latest
    env:
      JAVA_VERSION: "21"
    needs:
      - backend-release-plan
      - package-ios-sdk
      - package-android-sdk
    if: github.event_name == 'pull_request' || (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v')))
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Download iOS SDK artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: ios-sdk
          path: /tmp/pirate-ios-sdk-artifact

      - name: Download Android SDK artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: android-sdk
          path: /tmp/pirate-android-sdk-artifact

      - name: Stage native artifacts for React Native plugin
        run: |
          mkdir -p bindings/android-sdk/src/main/jniLibs bindings/ios-sdk/Frameworks dist/ios-sdk
          cp -R /tmp/pirate-android-sdk-artifact/bindings/android-sdk/src/main/jniLibs/. bindings/android-sdk/src/main/jniLibs/
          rm -rf bindings/ios-sdk/Frameworks/PirateWalletNative.xcframework
          cp -R /tmp/pirate-ios-sdk-artifact/bindings/ios-sdk/Frameworks/PirateWalletNative.xcframework bindings/ios-sdk/Frameworks/
          cp -R /tmp/pirate-ios-sdk-artifact/dist/ios-sdk/react-native dist/ios-sdk/
          chmod +x scripts/prepare-react-native-plugin.sh
          ./scripts/prepare-react-native-plugin.sh

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: ${{ env.NODE_VERSION }}
          package-manager-cache: false

      - name: Install pinned npm
        run: |
          npm install --global --ignore-scripts npm@${{ env.NPM_VERSION }}
          test "$(npm --version)" = "${{ env.NPM_VERSION }}"

      - name: Install React Native example app dependencies
        run: |
          cd bindings/react-native-pirate-wallet/example
          npm ci

      - name: Verify React Native package layout
        run: |
          cd bindings/react-native-pirate-wallet/example
          node ../scripts/verify-package.js
          node ../../react-native-pirate-wallet-android/scripts/verify-package.js
          node ../../react-native-pirate-wallet-android-x86_64/scripts/verify-package.js
          if [ "$(uname -s)" = "Darwin" ]; then
            node ../../react-native-pirate-wallet-ios-device/scripts/verify-package.js
            node ../../react-native-pirate-wallet-ios-simulator-arm64/scripts/verify-package.js
            node ../../react-native-pirate-wallet-ios-simulator-x86_64/scripts/verify-package.js
          fi
          node -e 'require("../react-native.config.js")'

      - name: Setup Java
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}

      - name: Setup Android SDK
        uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699

      - name: Install Android SDK packages
        run: |
          yes | sdkmanager "platforms;android-${{ env.ANDROID_PLATFORM_VERSION }}" "build-tools;${{ env.ANDROID_BUILD_TOOLS_VERSION }}"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb
        with:
          gradle-version: "8.13"

      - name: Smoke test React Native JS wrapper
        run: |
          node bindings/react-native-pirate-wallet/test/smoke.js

      - name: Run React Native example app test
        run: |
          cd bindings/react-native-pirate-wallet/example
          npm test -- --runInBand

      - name: Build Android example app for React Native plugin
        env:
          GRADLE_USER_HOME: ${{ runner.temp }}/gradle-rn-example-android
        run: |
          cd bindings/react-native-pirate-wallet/example/android
          chmod +x ./gradlew
          ./gradlew --no-daemon assembleDebug

      - name: Package React Native plugin
        run: |
          set -euo pipefail
          chmod +x scripts/package-react-native-plugin.sh
          scripts/package-react-native-plugin.sh

      - name: Upload React Native plugin package
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: react-native-pirate-wallet
          path: |
            dist/react-native-plugin/*.tgz
            dist/react-native-plugin/*.tgz.sha256
            dist/react-native-plugin/*.zip
            dist/react-native-plugin/*.zip.sha256
            dist/react-native-plugin/*-npm-pack.json

  verify-react-native-plugin-ios-example:
    name: Verify - React Native Plugin iOS Example
    runs-on: macos-15
    needs:
      - package-ios-sdk
      - package-android-sdk
    if: github.event_name == 'pull_request' || (github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v')))
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Download iOS SDK artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: ios-sdk
          path: /tmp/pirate-ios-sdk-artifact

      - name: Download Android SDK artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: android-sdk
          path: /tmp/pirate-android-sdk-artifact

      - name: Select Xcode
        run: |
          set -euo pipefail
          if [ -d /Applications/Xcode_26.3.app ]; then
            export DEVELOPER_DIR=/Applications/Xcode_26.3.app/Contents/Developer
          elif [ -d /Applications/Xcode_26.2.app ]; then
            export DEVELOPER_DIR=/Applications/Xcode_26.2.app/Contents/Developer
          else
            echo "No supported Xcode 26.x installation found on runner" >&2
            ls -1 /Applications | grep '^Xcode' || true
            exit 1
          fi
          echo "DEVELOPER_DIR=$DEVELOPER_DIR" >> "$GITHUB_ENV"
          xcodebuild -version

      - name: Stage native artifacts for React Native plugin
        run: |
          mkdir -p bindings/android-sdk/src/main/jniLibs bindings/ios-sdk/Frameworks dist/ios-sdk
          cp -R /tmp/pirate-android-sdk-artifact/bindings/android-sdk/src/main/jniLibs/. bindings/android-sdk/src/main/jniLibs/
          rm -rf bindings/ios-sdk/Frameworks/PirateWalletNative.xcframework
          cp -R /tmp/pirate-ios-sdk-artifact/bindings/ios-sdk/Frameworks/PirateWalletNative.xcframework bindings/ios-sdk/Frameworks/
          cp -R /tmp/pirate-ios-sdk-artifact/dist/ios-sdk/react-native dist/ios-sdk/
          chmod +x scripts/prepare-react-native-plugin.sh
          ./scripts/prepare-react-native-plugin.sh

      - name: Setup Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: ${{ env.NODE_VERSION }}
          package-manager-cache: false

      - name: Install pinned npm
        run: |
          npm install --global --ignore-scripts npm@${{ env.NPM_VERSION }}
          test "$(npm --version)" = "${{ env.NPM_VERSION }}"

      - name: Install React Native example app dependencies
        run: |
          cd bindings/react-native-pirate-wallet/example
          npm ci

      - name: Install CocoaPods
        run: gem install cocoapods -v ${{ env.COCOAPODS_VERSION }}

      - name: Verify CocoaPods
        run: |
          pod --version

      - name: Install iOS example app pods
        run: |
          cd bindings/react-native-pirate-wallet/example/ios
          pod install
          node ../../scripts/verify-package.js

      - name: Build iOS example app for React Native plugin
        run: |
          cd bindings/react-native-pirate-wallet/example/ios
          DESTINATION="$(xcrun simctl list devices available -j | python3 -c 'import json, sys; devices = json.load(sys.stdin).get("devices", {}); preferred = [("com.apple.CoreSimulator.SimRuntime.iOS-26-2", "iPhone 17"), ("com.apple.CoreSimulator.SimRuntime.iOS-26-2", "iPhone 16"), ("com.apple.CoreSimulator.SimRuntime.iOS-26-1", "iPhone 17"), ("com.apple.CoreSimulator.SimRuntime.iOS-26-1", "iPhone 16"), ("com.apple.CoreSimulator.SimRuntime.iOS-18-6", "iPhone 16"), ("com.apple.CoreSimulator.SimRuntime.iOS-18-5", "iPhone 16")]; available = [(runtime, device.get("name")) for runtime, entries in devices.items() if "iOS" in runtime for device in entries if device.get("isAvailable")]; chosen = next((name for runtime_name, device_name in preferred for runtime, name in available if runtime == runtime_name and name == device_name), None); chosen = chosen or (available[0][1] if available else ""); print("platform=iOS Simulator,name=" + chosen); sys.exit(0 if chosen else 1)')"
          rm -rf /tmp/pirate-rn-example-ios
          xcodebuild \
            -workspace PirateWalletPluginExample.xcworkspace \
            -scheme PirateWalletPluginExample \
            -destination "$DESTINATION" \
            -derivedDataPath /tmp/pirate-rn-example-ios \
            build

  # ========================================================================
  # Final Status Check
  # ========================================================================

  # ========================================================================
  # Reproducible Packaging & SBOM
  # ========================================================================

  package-android:
    name: Package - Android (APK/AAB + SBOM)
    runs-on: ubuntu-latest
    env:
      REPRODUCIBLE: "1"
      JAVA_VERSION: "21"
      GRADLE_OPTS: "-Dorg.gradle.vfs.watch=false"
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v'))
    needs:
      - rust-test
      - flutter-test
      - security-audit
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0  # Full history for SOURCE_DATE_EPOCH

      - name: Setup Java
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}

      - name: Setup Android SDK
        uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699

      - name: Install Android SDK packages
        run: |
          yes | sdkmanager "platforms;android-${{ env.ANDROID_PLATFORM_VERSION }}" "build-tools;${{ env.ANDROID_BUILD_TOOLS_VERSION }}" "ndk;${{ env.ANDROID_NDK_VERSION }}"
          echo "ANDROID_NDK_HOME=${ANDROID_SDK_ROOT}/ndk/${{ env.ANDROID_NDK_VERSION }}" >> $GITHUB_ENV
          echo "ANDROID_NDK_ROOT=${ANDROID_SDK_ROOT}/ndk/${{ env.ANDROID_NDK_VERSION }}" >> $GITHUB_ENV

      - name: Setup Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
          cache-key: flutter-${{ runner.os }}-${{ runner.arch }}-${{ env.FLUTTER_VERSION }}-sdk-v2
          pub-cache-key: flutter-pub-${{ runner.os }}-${{ runner.arch }}-${{ env.FLUTTER_VERSION }}-v2

      - name: Setup Java
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: false

      - name: Setup Java
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libsqlite3-dev libssl-dev pkg-config

      - name: Set SOURCE_DATE_EPOCH
        run: |
          echo "SOURCE_DATE_EPOCH=$(git log -1 --format=%ct)" >> $GITHUB_ENV

      - name: Verify toolchain pins
        run: |
          chmod +x scripts/verify-toolchain.sh
          scripts/verify-toolchain.sh

      - name: Build Android APK
        run: |
          chmod +x scripts/build-android.sh
          REPRODUCIBLE=1 ./scripts/build-android.sh apk

      - name: Build Android AAB
        run: |
          REPRODUCIBLE=1 ./scripts/build-android.sh bundle

      - name: Reclaim workspace disk
        run: |
          chmod +x scripts/cleanup-build-intermediates.sh
          ./scripts/cleanup-build-intermediates.sh

      - name: Generate SBOM
        run: |
          chmod +x scripts/generate-sbom.sh
          ./scripts/generate-sbom.sh dist/sbom

      - name: Generate Provenance
        run: |
          chmod +x scripts/generate-provenance.sh
          ./scripts/generate-provenance.sh dist/android/Stashi-Wallet-android-V8-unsigned.apk dist/provenance
          ./scripts/generate-provenance.sh dist/android/Stashi-Wallet-android-unsigned.aab dist/provenance

      - name: Upload APK (V8)
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: android-apk-unsigned-v8
          path: |
            dist/android/Stashi-Wallet-android-V8-unsigned.apk
            dist/android/Stashi-Wallet-android-V8-unsigned.apk.sha256

      - name: Upload APK (V7)
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: android-apk-unsigned-v7
          path: |
            dist/android/Stashi-Wallet-android-V7-unsigned.apk
            dist/android/Stashi-Wallet-android-V7-unsigned.apk.sha256

      - name: Upload AAB
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: android-aab
          path: |
            dist/android/*.aab
            dist/android/*.aab.sha256

      - name: Upload SBOM
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: android-sbom
          path: dist/sbom/**/*

      - name: Upload Provenance
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: android-provenance
          path: dist/provenance/**/*

  package-ios:
    name: Package - iOS (IPA + SBOM)
    runs-on: macos-15
    env:
      REPRODUCIBLE: "1"
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v'))
    needs:
      - rust-test
      - flutter-test
      - security-audit
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0

      - name: Setup Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
          cache-key: flutter-${{ runner.os }}-${{ runner.arch }}-${{ env.FLUTTER_VERSION }}-sdk-v2
          pub-cache-key: flutter-pub-${{ runner.os }}-${{ runner.arch }}-${{ env.FLUTTER_VERSION }}-v2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Setup Java
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}

      - name: Select Xcode 26
        run: |
          set -euo pipefail
          if [ -d /Applications/Xcode_26.3.app ]; then
            export DEVELOPER_DIR=/Applications/Xcode_26.3.app/Contents/Developer
          elif [ -d /Applications/Xcode_26.2.app ]; then
            export DEVELOPER_DIR=/Applications/Xcode_26.2.app/Contents/Developer
          elif [ -d /Applications/Xcode_26.1.app ]; then
            export DEVELOPER_DIR=/Applications/Xcode_26.1.app/Contents/Developer
          elif [ -d /Applications/Xcode_26.app ]; then
            export DEVELOPER_DIR=/Applications/Xcode_26.app/Contents/Developer
          else
            echo "Xcode 26 is required for App Store Connect iOS uploads." >&2
            ls -1 /Applications | grep '^Xcode' >&2 || true
            exit 1
          fi
          echo "DEVELOPER_DIR=$DEVELOPER_DIR" >> "$GITHUB_ENV"
          xcodebuild -version

      - name: Install CocoaPods
        run: gem install cocoapods -v ${{ env.COCOAPODS_VERSION }}

      - name: Set SOURCE_DATE_EPOCH
        run: |
          echo "SOURCE_DATE_EPOCH=$(git log -1 --format=%ct)" >> $GITHUB_ENV

      - name: Verify toolchain pins
        run: |
          chmod +x scripts/verify-toolchain.sh
          scripts/verify-toolchain.sh

      - name: Build iOS IPA
        env:
          GITHUB_API_PUBLIC_READONLY_TOKEN: ${{ github.token }}
        run: |
          chmod +x scripts/build-ios.sh
          REPRODUCIBLE=1 ./scripts/build-ios.sh false || {
            echo "iOS build debug: locating Runner.app output"
            if [ -d app/build ]; then
              find app/build -maxdepth 4 -type d -name "iphoneos" -print
              find app/build -path "*/ios/iphoneos/Runner.app" -type d -print
              ls -la app/build/ios || true
              ls -la app/build/ios/iphoneos || true
            else
              echo "app/build does not exist"
            fi
            exit 1
          }

      # komodo_defi_framework currently ships KDF for physical iOS devices only
      # (ios-aarch64). Do not build/package an iOS simulator app until the SDK
      # provides an iphonesimulator slice or an xcframework containing one.

      - name: Reclaim workspace disk
        run: |
          chmod +x scripts/cleanup-build-intermediates.sh
          ./scripts/cleanup-build-intermediates.sh

      - name: Generate SBOM
        run: |
          chmod +x scripts/generate-sbom.sh
          ./scripts/generate-sbom.sh dist/sbom

      - name: Generate Provenance
        run: |
          chmod +x scripts/generate-provenance.sh
          ./scripts/generate-provenance.sh dist/ios/Stashi-Wallet-ios-unsigned.ipa dist/provenance

      - name: Upload IPA
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: ios-ipa
          path: |
            dist/ios/*.ipa
            dist/ios/*.ipa.sha256

      - name: Upload SBOM
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: ios-sbom
          path: dist/sbom/**/*

      - name: Upload Provenance
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: ios-provenance
          path: dist/provenance/**/*

  package-windows:
    name: Package - Windows (Installer + Portable + SBOM)
    runs-on: windows-2025-vs2026
    env:
      REPRODUCIBLE: "1"
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v'))
    needs:
      - rust-test
      - flutter-test
      - security-audit
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0

      - name: Setup Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
          cache-key: flutter-${{ runner.os }}-${{ runner.arch }}-${{ env.FLUTTER_VERSION }}-sdk-v2
          pub-cache-key: flutter-pub-${{ runner.os }}-${{ runner.arch }}-${{ env.FLUTTER_VERSION }}-v2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: false

      - name: Setup Java
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}

      - name: Install Perl (OpenSSL build)
        shell: pwsh
        run: |
          choco install strawberryperl --version 5.42.0.1 --no-progress -y
          "C:\Strawberry\perl\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding ascii -Append

      - name: Install Inno Setup
        shell: pwsh
        run: |
          choco install innosetup --no-progress -y
          "C:\Program Files (x86)\Inno Setup 6" | Out-File -FilePath $env:GITHUB_PATH -Encoding ascii -Append

      - name: Set SOURCE_DATE_EPOCH
        run: |
          $epoch = git log -1 --format=%ct
          echo "SOURCE_DATE_EPOCH=$epoch" >> $env:GITHUB_ENV

      - name: Verify toolchain pins
        shell: bash
        run: |
          chmod +x scripts/verify-toolchain.sh
          scripts/verify-toolchain.sh

      - name: Build Windows desktop artifacts
        shell: bash
        env:
          GITHUB_API_PUBLIC_READONLY_TOKEN: ${{ github.token }}
        run: |
          chmod +x scripts/build-windows.sh
          REPRODUCIBLE=1 ./scripts/build-windows.sh

      - name: Reclaim workspace disk
        shell: bash
        run: |
          chmod +x scripts/cleanup-build-intermediates.sh
          ./scripts/cleanup-build-intermediates.sh

      - name: Generate SBOM
        shell: bash
        env:
          PERL: C:\Strawberry\perl\bin\perl.exe
        run: |
          export PATH="/c/Strawberry/perl/bin:/c/Strawberry/c/bin:$PATH"
          chmod +x scripts/generate-sbom.sh
          ./scripts/generate-sbom.sh dist/sbom

      - name: Generate Provenance
        shell: bash
        run: |
          chmod +x scripts/generate-provenance.sh
          if [ -f dist/windows/Stashi-Wallet-windows-installer-unsigned.exe ]; then
            ./scripts/generate-provenance.sh dist/windows/Stashi-Wallet-windows-installer-unsigned.exe dist/provenance
          fi
          if [ -f dist/windows/Stashi-Wallet-windows-portable-unsigned.zip ]; then
            ./scripts/generate-provenance.sh dist/windows/Stashi-Wallet-windows-portable-unsigned.zip dist/provenance
          fi

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: windows-desktop
          path: |
            dist/windows/*.exe
            dist/windows/*.zip
            dist/windows/*.sha256
            dist/windows/installed-payload-windows.txt

      - name: Upload SBOM
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: windows-sbom
          path: dist/sbom/**/*

      - name: Upload Provenance
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: windows-provenance
          path: dist/provenance/**/*

  package-macos:
    name: Package - macOS (DMG + SBOM)
    runs-on: macos-14
    env:
      REPRODUCIBLE: "1"
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v'))
    needs:
      - rust-test
      - flutter-test
      - security-audit
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0

      - name: Setup Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
          cache-key: flutter-${{ runner.os }}-${{ runner.arch }}-${{ env.FLUTTER_VERSION }}-sdk-v2
          pub-cache-key: flutter-pub-${{ runner.os }}-${{ runner.arch }}-${{ env.FLUTTER_VERSION }}-v2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: false

      - name: Setup Java
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}

      - name: Install CocoaPods
        run: gem install cocoapods -v ${{ env.COCOAPODS_VERSION }}

      - name: Set SOURCE_DATE_EPOCH
        run: |
          echo "SOURCE_DATE_EPOCH=$(git log -1 --format=%ct)" >> $GITHUB_ENV

      - name: Verify toolchain pins
        run: |
          chmod +x scripts/verify-toolchain.sh
          scripts/verify-toolchain.sh

      - name: Build macOS DMG
        env:
          GITHUB_API_PUBLIC_READONLY_TOKEN: ${{ github.token }}
        run: |
          chmod +x scripts/build-macos.sh
          REPRODUCIBLE=1 ./scripts/build-macos.sh false

      - name: Verify macOS DMG
        run: |
          chmod +x scripts/verify-macos-dmg.sh
          shopt -s nullglob
          dmgs=(dist/macos/*.dmg)
          if [ "${#dmgs[@]}" -eq 0 ]; then
            echo "No DMG artifacts found in dist/macos/" >&2
            exit 1
          fi
          for dmg in "${dmgs[@]}"; do
            scripts/verify-macos-dmg.sh "$dmg"
          done

      - name: Reclaim workspace disk
        run: |
          chmod +x scripts/cleanup-build-intermediates.sh
          ./scripts/cleanup-build-intermediates.sh

      - name: Generate SBOM
        run: |
          chmod +x scripts/generate-sbom.sh
          ./scripts/generate-sbom.sh dist/sbom

      - name: Generate Provenance
        run: |
          chmod +x scripts/generate-provenance.sh
          ./scripts/generate-provenance.sh dist/macos/Stashi-Wallet-macos-unsigned.dmg dist/provenance

      - name: Upload DMG
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: macos-dmg
          path: |
            dist/macos/*.dmg
            dist/macos/*.dmg.sha256
            dist/macos/installed-payload-macos-unsigned.txt

      - name: Upload SBOM
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: macos-sbom
          path: dist/sbom/**/*

      - name: Upload Provenance
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: macos-provenance
          path: dist/provenance/**/*

  package-linux:
    name: Package - Linux (AppImage/Flatpak/Deb + SBOM)
    runs-on: ubuntu-22.04
    env:
      REPRODUCIBLE: "1"
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || startsWith(github.ref, 'refs/tags/v'))
    needs:
      - rust-test
      - flutter-test
      - security-audit
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0

      - name: Setup Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
          cache-key: flutter-${{ runner.os }}-${{ runner.arch }}-${{ env.FLUTTER_VERSION }}-sdk-v2
          pub-cache-key: flutter-pub-${{ runner.os }}-${{ runner.arch }}-${{ env.FLUTTER_VERSION }}-v2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Setup Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: false

      - name: Setup Java
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            libboost-program-options-dev \
            libgtk-3-dev \
            libglib2.0-dev \
            libsecret-1-dev \
            libsqlite3-dev \
            libssl-dev \
            zlib1g-dev \
            pkg-config \
            clang \
            cmake \
            ninja-build \
            libblkid-dev \
            liblzma-dev \
            dpkg-dev \
            squashfs-tools \
            flatpak \
            flatpak-builder

      - name: Set SOURCE_DATE_EPOCH
        run: |
          echo "SOURCE_DATE_EPOCH=$(git log -1 --format=%ct)" >> $GITHUB_ENV

      - name: Verify toolchain pins
        run: |
          chmod +x scripts/verify-toolchain.sh
          scripts/verify-toolchain.sh

      - name: Build AppImage
        env:
          GITHUB_API_PUBLIC_READONLY_TOKEN: ${{ github.token }}
        run: |
          chmod +x scripts/build-linux.sh
          REPRODUCIBLE=1 ./scripts/build-linux.sh appimage

      - name: Build Flatpak manifest
        env:
          GITHUB_API_PUBLIC_READONLY_TOKEN: ${{ github.token }}
        run: |
          REPRODUCIBLE=1 ./scripts/build-linux.sh flatpak

      - name: Build Debian package
        env:
          GITHUB_API_PUBLIC_READONLY_TOKEN: ${{ github.token }}
        run: |
          REPRODUCIBLE=1 ./scripts/build-linux.sh deb

      - name: Reclaim workspace disk
        run: |
          chmod +x scripts/cleanup-build-intermediates.sh
          ./scripts/cleanup-build-intermediates.sh

      - name: Generate SBOM
        run: |
          chmod +x scripts/generate-sbom.sh
          ./scripts/generate-sbom.sh dist/sbom

      - name: Generate Provenance
        run: |
          chmod +x scripts/generate-provenance.sh
          ./scripts/generate-provenance.sh dist/linux/Stashi-Wallet-linux-x86_64.AppImage dist/provenance
          ./scripts/generate-provenance.sh dist/linux/Stashi-Wallet-amd64.deb dist/provenance

      - name: Upload AppImage
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: linux-appimage
          path: |
            dist/linux/*.AppImage
            dist/linux/*.AppImage.sha256
            dist/linux/installed-payload-linux.txt

      - name: Upload Flatpak
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: linux-flatpak
          path: |
            dist/linux/*.flatpak
            com.pirate.wallet.yml

      - name: Upload Debian Package
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: linux-deb
          path: |
            dist/linux/*.deb
            dist/linux/*.deb.sha256
            dist/linux/apt-repo/**/*

      - name: Upload SBOM
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: linux-sbom
          path: dist/sbom/**/*

      - name: Upload Provenance
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: linux-provenance
          path: dist/provenance/**/*

  # ========================================================================
  # Signed Release Artifacts (Non-Reproducible)
  # ========================================================================

  sign-android:
    name: Sign - Android (APK/AAB)
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    env:
      GRADLE_OPTS: "-Dorg.gradle.vfs.watch=false"
    needs:
      - package-android
    steps:
      - name: Check signing secrets
        id: signing
        run: |
          if [ -n "${{ secrets.ANDROID_KEYSTORE_B64 }}" ] && [ -n "${{ secrets.ANDROID_KEYSTORE_PASSWORD }}" ] && [ -n "${{ secrets.ANDROID_KEY_ALIAS }}" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Java
        if: steps.signing.outputs.enabled == 'true'
        uses: actions/setup-java@dd06d9cba3e5552c54d9f8ea23572deb30010f7c
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}

      - name: Setup Android SDK
        if: steps.signing.outputs.enabled == 'true'
        uses: android-actions/setup-android@40fd30fb8d7440372e1316f5d1809ec01dcd3699

      - name: Install Android Build Tools
        if: steps.signing.outputs.enabled == 'true'
        run: |
          yes | sdkmanager "build-tools;${{ env.ANDROID_BUILD_TOOLS_VERSION }}"

      - name: Download APK artifacts (V8)
        if: steps.signing.outputs.enabled == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: android-apk-unsigned-v8
          path: dist/android

      - name: Download APK artifacts (V7)
        if: steps.signing.outputs.enabled == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: android-apk-unsigned-v7
          path: dist/android

      - name: Download AAB artifacts
        if: steps.signing.outputs.enabled == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: android-aab
          path: dist/android

      - name: Decode keystore
        if: steps.signing.outputs.enabled == 'true'
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_B64 }}" | base64 -d > "$RUNNER_TEMP/keystore.jks"

      - name: Sign APK and AAB
        if: steps.signing.outputs.enabled == 'true'
        env:
          KEYSTORE_PATH: ${{ runner.temp }}/keystore.jks
          KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          set -euo pipefail
          KEY_PASSWORD="${KEY_PASSWORD:-$KEYSTORE_PASSWORD}"
          APKSIGNER="$ANDROID_SDK_ROOT/build-tools/${{ env.ANDROID_BUILD_TOOLS_VERSION }}/apksigner"
          if [ ! -f "$APKSIGNER" ]; then
            echo "apksigner not found at $APKSIGNER" >&2
            exit 1
          fi
          mapfile -t APK_UNSIGNED_LIST < <(ls dist/android/*-unsigned.apk 2>/dev/null || true)
          if [ "${#APK_UNSIGNED_LIST[@]}" -eq 0 ]; then
            echo "No unsigned APKs found in dist/android" >&2
            exit 1
          fi
          for APK_UNSIGNED in "${APK_UNSIGNED_LIST[@]}"; do
            APK_SIGNED="${APK_UNSIGNED/-unsigned.apk/.apk}"
            echo "Signing $APK_UNSIGNED -> $APK_SIGNED"
            "$APKSIGNER" sign \
              --ks "$KEYSTORE_PATH" \
              --ks-key-alias "$KEY_ALIAS" \
              --ks-pass "pass:$KEYSTORE_PASSWORD" \
              --key-pass "pass:$KEY_PASSWORD" \
              --out "$APK_SIGNED" \
              "$APK_UNSIGNED"
            "$APKSIGNER" verify --verbose "$APK_SIGNED"
            sha256sum "$APK_SIGNED" > "$APK_SIGNED.sha256"
          done

          AAB_UNSIGNED="dist/android/Stashi-Wallet-android-unsigned.aab"
          AAB_SIGNED="dist/android/Stashi-Wallet-android.aab"
          if [ ! -f "$AAB_UNSIGNED" ]; then
            echo "Unsigned AAB not found at $AAB_UNSIGNED" >&2
            exit 1
          fi
          cp "$AAB_UNSIGNED" "$AAB_SIGNED"
          jarsigner -verbose \
            -sigalg SHA256withRSA \
            -digestalg SHA-256 \
            -keystore "$KEYSTORE_PATH" \
            -storepass "$KEYSTORE_PASSWORD" \
            -keypass "$KEY_PASSWORD" \
            "$AAB_SIGNED" \
            "$KEY_ALIAS"
          sha256sum "$AAB_SIGNED" > "$AAB_SIGNED.sha256"

      - name: Upload signed APK (V8)
        if: steps.signing.outputs.enabled == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: android-apk-signed-v8
          path: |
            dist/android/Stashi-Wallet-android-V8.apk
            dist/android/Stashi-Wallet-android-V8.apk.sha256
          if-no-files-found: error

      - name: Upload signed APK (V7)
        if: steps.signing.outputs.enabled == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: android-apk-signed-v7
          path: |
            dist/android/Stashi-Wallet-android-V7.apk
            dist/android/Stashi-Wallet-android-V7.apk.sha256
          if-no-files-found: error

      - name: Upload signed AAB
        if: steps.signing.outputs.enabled == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: android-aab-signed
          path: |
            dist/android/Stashi-Wallet-android.aab
            dist/android/Stashi-Wallet-android.aab.sha256
          if-no-files-found: error

  sign-windows:
    name: Sign - Windows (Installer)
    runs-on: windows-2025-vs2026
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs:
      - package-windows
    steps:
      - name: Check signing secrets
        id: signing
        shell: pwsh
        run: |
          if ("${{ secrets.WINDOWS_SIGN_CERT_PFX_B64 }}" -and "${{ secrets.WINDOWS_SIGN_CERT_PASSWORD }}") {
            "enabled=true" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding ascii -Append
          } else {
            "enabled=false" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding ascii -Append
          }

      - name: Download Windows artifacts
        if: steps.signing.outputs.enabled == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: windows-desktop
          path: dist/windows

      - name: Decode Windows signing certificate
        if: steps.signing.outputs.enabled == 'true'
        shell: pwsh
        run: |
          $certPath = Join-Path $env:RUNNER_TEMP "windows-signing.pfx"
          [IO.File]::WriteAllBytes($certPath, [Convert]::FromBase64String("${{ secrets.WINDOWS_SIGN_CERT_PFX_B64 }}"))
          "WINDOWS_SIGN_CERT=$certPath" | Out-File -FilePath $env:GITHUB_ENV -Encoding ascii -Append

      - name: Sign Windows installer
        if: steps.signing.outputs.enabled == 'true'
        shell: pwsh
        run: |
          $signtoolPath = $null
          $signtool = (Get-Command signtool.exe -ErrorAction SilentlyContinue)
          if ($signtool) { $signtoolPath = $signtool.Source }
          if (-not $signtoolPath) {
            $signtool = (Get-Command signtool -ErrorAction SilentlyContinue)
            if ($signtool) { $signtoolPath = $signtool.Source }
          }
          if (-not $signtoolPath) {
            $kitRoots = @()
            $pf86 = [Environment]::GetEnvironmentVariable('ProgramFiles(x86)')
            if ($pf86) { $kitRoots += (Join-Path $pf86 'Windows Kits\\10\\bin') }
            $pf = [Environment]::GetEnvironmentVariable('ProgramFiles')
            if ($pf) { $kitRoots += (Join-Path $pf 'Windows Kits\\10\\bin') }
            $kitRoots += 'C:\\Program Files (x86)\\Windows Kits\\10\\bin'
            $kitRoots += 'C:\\Program Files\\Windows Kits\\10\\bin'
            $kitRoots = $kitRoots | Select-Object -Unique
            foreach ($kitRoot in $kitRoots) {
              if (-not (Test-Path $kitRoot)) { continue }
              $signtoolCandidate = Get-ChildItem -Path (Join-Path $kitRoot '10.*\\x64\\signtool.exe') -ErrorAction SilentlyContinue |
                Select-Object -First 1
              if (-not $signtoolCandidate) {
                $versionDirs = Get-ChildItem -Path $kitRoot -Directory -ErrorAction SilentlyContinue |
                  Sort-Object Name -Descending
                foreach ($dir in $versionDirs) {
                  $candidate = Join-Path $dir.FullName 'x64\\signtool.exe'
                  if (Test-Path $candidate) {
                    $signtoolPath = $candidate
                    break
                  }
                }
              } else {
                $signtoolPath = $signtoolCandidate.FullName
              }
              if ($signtoolPath) { break }
            }
          }
          if (-not $signtoolPath) {
            Write-Host "signtool not found. Attempting to install Windows SDK..."
            choco install windows-sdk-10.0.22621.0 -y --no-progress
            $kitRoot = 'C:\\Program Files (x86)\\Windows Kits\\10\\bin'
            if (Test-Path $kitRoot) {
              $signtoolCandidate = Get-ChildItem -Path (Join-Path $kitRoot '10.*\\x64\\signtool.exe') -ErrorAction SilentlyContinue |
                Select-Object -First 1
              if ($signtoolCandidate) { $signtoolPath = $signtoolCandidate.FullName }
            }
          }
          if (-not $signtoolPath) {
            Write-Error "signtool not found on PATH or in Windows Kits"
            exit 1
          }

          $cert = $env:WINDOWS_SIGN_CERT
          $pass = "${{ secrets.WINDOWS_SIGN_CERT_PASSWORD }}"

          $installerUnsigned = "dist/windows/Stashi-Wallet-windows-installer-unsigned.exe"
          $installerSigned = "dist/windows/Stashi-Wallet-windows-installer.exe"
          if (-not (Test-Path $installerUnsigned)) {
            Write-Error "Unsigned installer not found"
            exit 1
          }
          Copy-Item $installerUnsigned $installerSigned -Force
          & $signtoolPath sign /fd SHA256 /f $cert /p $pass $installerSigned
          if ($LASTEXITCODE -ne 0) {
            Write-Error "Windows installer signing failed"
            exit $LASTEXITCODE
          }
          (Get-FileHash -Algorithm SHA256 $installerSigned).Hash | Set-Content -Encoding ascii "$installerSigned.sha256"

      - name: Upload signed Windows artifacts
        if: steps.signing.outputs.enabled == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: windows-signed
          path: |
            dist/windows/Stashi-Wallet-windows-installer.exe
            dist/windows/Stashi-Wallet-windows-installer.exe.sha256
          if-no-files-found: error

  sign-macos:
    name: Sign - macOS (DMG)
    runs-on: macos-14
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs:
      - package-macos
    steps:
      - name: Check macOS signing configuration
        id: signing
        env:
          SIGNING_ENABLED: ${{ vars.MACOS_SIGNING_ENABLED }}
          SIGN_CERT_P12_B64: ${{ secrets.MACOS_SIGN_CERT_P12_B64 }}
          SIGN_CERT_PASSWORD: ${{ secrets.MACOS_SIGN_CERT_PASSWORD }}
          APPLE_ID: ${{ secrets.MACOS_APPLE_ID }}
          TEAM_ID: ${{ secrets.MACOS_TEAM_ID }}
          APP_PASSWORD: ${{ secrets.MACOS_APP_PASSWORD }}
        run: |
          if [ "$SIGNING_ENABLED" != "true" ]; then
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "::notice::macOS signing is disabled. The release will use the unsigned macOS DMG."
            exit 0
          fi

          if [ -z "$SIGN_CERT_P12_B64" ] || [ -z "$SIGN_CERT_PASSWORD" ] || [ -z "$APPLE_ID" ] || [ -z "$TEAM_ID" ] || [ -z "$APP_PASSWORD" ]; then
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "::error::MACOS_SIGNING_ENABLED is true, but one or more macOS signing/notarization secrets are missing."
            exit 1
          fi

          echo "enabled=true" >> "$GITHUB_OUTPUT"
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        if: steps.signing.outputs.enabled == 'true'
        with:
          fetch-depth: 0

      - name: Setup Flutter
        if: steps.signing.outputs.enabled == 'true'
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
          cache-key: flutter-${{ runner.os }}-${{ runner.arch }}-${{ env.FLUTTER_VERSION }}-sdk-v2
          pub-cache-key: flutter-pub-${{ runner.os }}-${{ runner.arch }}-${{ env.FLUTTER_VERSION }}-v2

      - name: Setup Rust
        if: steps.signing.outputs.enabled == 'true'
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Setup Go
        if: steps.signing.outputs.enabled == 'true'
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: false

      - name: Install CocoaPods
        if: steps.signing.outputs.enabled == 'true'
        run: gem install cocoapods -v ${{ env.COCOAPODS_VERSION }}

      - name: Import macOS signing certificate
        if: steps.signing.outputs.enabled == 'true'
        env:
          CERT_P12_B64: ${{ secrets.MACOS_SIGN_CERT_P12_B64 }}
          CERT_PASSWORD: ${{ secrets.MACOS_SIGN_CERT_PASSWORD }}
        run: |
          CERT_PATH="$RUNNER_TEMP/macos-signing.p12"
          printf '%s' "$CERT_P12_B64" | base64 -d > "$CERT_PATH"
          CERT_PASSWORD="$(printf '%s' "$CERT_PASSWORD" | tr -d '\r\n')"
          if ! openssl pkcs12 -in "$CERT_PATH" -passin pass:"$CERT_PASSWORD" -nokeys -clcerts -out /dev/null >/dev/null 2>&1; then
            echo "::error::Unable to decrypt macOS signing certificate. Recheck MACOS_SIGN_CERT_P12_B64 and MACOS_SIGN_CERT_PASSWORD are from the same .p12 export."
            exit 1
          fi
          KEYCHAIN="$RUNNER_TEMP/macos-signing.keychain-db"
          KEYCHAIN_PASSWORD="$(uuidgen)"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security set-keychain-settings -t 3600 -u "$KEYCHAIN"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security import "$CERT_PATH" -k "$KEYCHAIN" -P "$CERT_PASSWORD" -T /usr/bin/codesign -T /usr/bin/security
          security list-keychains -s "$KEYCHAIN"
          security set-key-partition-list -S apple-tool:,apple: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          echo "MACOS_SIGN_KEYCHAIN=$KEYCHAIN" >> "$GITHUB_ENV"
          echo "MACOS_SIGN_KEYCHAIN_PASSWORD=$KEYCHAIN_PASSWORD" >> "$GITHUB_ENV"

      - name: Resolve macOS signing identity
        if: steps.signing.outputs.enabled == 'true'
        run: |
          KEYCHAIN="${MACOS_SIGN_KEYCHAIN:-$RUNNER_TEMP/macos-signing.keychain-db}"
          if [ -f "$KEYCHAIN" ] && [ -n "${MACOS_SIGN_KEYCHAIN_PASSWORD:-}" ]; then
            security unlock-keychain -p "$MACOS_SIGN_KEYCHAIN_PASSWORD" "$KEYCHAIN"
            security list-keychains -s "$KEYCHAIN"
            security default-keychain -s "$KEYCHAIN"
          fi

          if [ -n "${MACOS_SIGN_IDENTITY:-}" ]; then
            echo "MACOS_SIGN_IDENTITY=$MACOS_SIGN_IDENTITY" >> "$GITHUB_ENV"
            exit 0
          fi

          if [ -n "${{ secrets.MACOS_SIGN_IDENTITY }}" ]; then
            echo "MACOS_SIGN_IDENTITY=${{ secrets.MACOS_SIGN_IDENTITY }}" >> "$GITHUB_ENV"
            exit 0
          fi

          if [ -f "$KEYCHAIN" ]; then
            IDENTITY="$(security find-identity -v -p codesigning "$KEYCHAIN" | awk -F '\"' 'NR==1 {print $2}')"
          else
            IDENTITY="$(security find-identity -v -p codesigning | awk -F '\"' 'NR==1 {print $2}')"
          fi
          if [ -z "$IDENTITY" ]; then
            echo "No code signing identity found after import" >&2
            if [ -f "$KEYCHAIN" ]; then
              security find-identity -v -p codesigning "$KEYCHAIN" || true
            else
              security find-identity -v -p codesigning || true
            fi
            exit 1
          fi
          echo "MACOS_SIGN_IDENTITY=$IDENTITY" >> "$GITHUB_ENV"

      - name: Build signed macOS DMG
        if: steps.signing.outputs.enabled == 'true'
        env:
          GITHUB_API_PUBLIC_READONLY_TOKEN: ${{ github.token }}
          MACOS_NOTARIZE: "true"
          MACOS_NOTARY_TIMEOUT_SECONDS: "60"
          MACOS_NOTARY_POLL_SECONDS: "20"
          MACOS_NOTARY_ALLOW_PENDING: "true"
          MACOS_APPLE_ID: ${{ secrets.MACOS_APPLE_ID }}
          MACOS_TEAM_ID: ${{ secrets.MACOS_TEAM_ID }}
          MACOS_APP_PASSWORD: ${{ secrets.MACOS_APP_PASSWORD }}
        run: |
          chmod +x scripts/build-macos.sh
          REPRODUCIBLE=0 ./scripts/build-macos.sh true

      - name: Classify macOS notarization result
        id: macos_notary
        if: steps.signing.outputs.enabled == 'true'
        run: |
          set -euo pipefail
          metadata="dist/macos/Stashi-Wallet-macos.notary.json"
          status=""
          if [ -f "$metadata" ]; then
            status="$(python3 -c 'import json, pathlib, sys; print(json.loads(pathlib.Path(sys.argv[1]).read_text(encoding="utf-8")).get("status", ""))' "$metadata")"
          fi
          echo "status=${status:-unknown}" >> "$GITHUB_OUTPUT"
          if [ "$status" = "Accepted" ]; then
            echo "final=true" >> "$GITHUB_OUTPUT"
          else
            echo "final=false" >> "$GITHUB_OUTPUT"
            dmg="dist/macos/Stashi-Wallet-macos.dmg"
            pending="dist/macos/Stashi-Wallet-macos-pending-notarization.dmg"
            if [ -f "$dmg" ]; then
              mv "$dmg" "$pending"
              shasum -a 256 "$pending" > "$pending.sha256"
              rm -f "$dmg.sha256"
            fi
          fi

      - name: Upload signed DMG
        if: steps.signing.outputs.enabled == 'true' && steps.macos_notary.outputs.final == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: macos-dmg-signed
          path: |
            dist/macos/Stashi-Wallet-macos.dmg
            dist/macos/Stashi-Wallet-macos.dmg.sha256
            dist/macos/Stashi-Wallet-macos.notary.json
            dist/macos/installed-payload-macos.txt
          if-no-files-found: error

      - name: Upload pending macOS notarization artifact
        if: steps.signing.outputs.enabled == 'true' && steps.macos_notary.outputs.final != 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: macos-dmg-notary-pending
          path: |
            dist/macos/Stashi-Wallet-macos-pending-notarization.dmg
            dist/macos/Stashi-Wallet-macos-pending-notarization.dmg.sha256
            dist/macos/Stashi-Wallet-macos.notary.json
            dist/macos/installed-payload-macos.txt
          if-no-files-found: error

  sign-ios:
    name: Sign - iOS (IPA)
    runs-on: macos-15
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs:
      - package-ios
    steps:
      - name: Check iOS signing configuration
        id: signing
        env:
          SIGNING_ENABLED: ${{ vars.IOS_SIGNING_ENABLED }}
          SIGN_CERT_P12_B64: ${{ secrets.IOS_SIGNING_CERT_P12_B64 }}
          SIGN_CERT_PASSWORD: ${{ secrets.IOS_SIGNING_CERT_PASSWORD }}
          PROVISIONING_PROFILE_B64: ${{ secrets.IOS_PROVISIONING_PROFILE_B64 }}
          EXPORT_OPTIONS_PLIST_B64: ${{ secrets.IOS_EXPORT_OPTIONS_PLIST_B64 }}
        run: |
          if [ "$SIGNING_ENABLED" != "true" ]; then
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "::notice::iOS signing is disabled. The unsigned IPA will remain in the mobile store/test-build bundle."
            exit 0
          fi

          if [ -z "$SIGN_CERT_P12_B64" ] || [ -z "$SIGN_CERT_PASSWORD" ] || [ -z "$PROVISIONING_PROFILE_B64" ] || [ -z "$EXPORT_OPTIONS_PLIST_B64" ]; then
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "::error::IOS_SIGNING_ENABLED is true, but one or more iOS signing secrets are missing."
            exit 1
          fi

          echo "enabled=true" >> "$GITHUB_OUTPUT"

      - name: Check TestFlight upload secrets
        id: testflight
        if: steps.signing.outputs.enabled == 'true'
        run: |
          if [ -n "${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}" ] && [ -n "${{ secrets.APP_STORE_CONNECT_API_ISSUER_ID }}" ] && [ -n "${{ secrets.APP_STORE_CONNECT_API_KEY_P8_B64 }}" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Missing App Store Connect API secrets. Signed IPA will be produced but not uploaded to TestFlight." >&2
          fi

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        if: steps.signing.outputs.enabled == 'true'
        with:
          fetch-depth: 0

      - name: Setup Flutter
        if: steps.signing.outputs.enabled == 'true'
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
          cache-key: flutter-${{ runner.os }}-${{ runner.arch }}-${{ env.FLUTTER_VERSION }}-sdk-v2
          pub-cache-key: flutter-pub-${{ runner.os }}-${{ runner.arch }}-${{ env.FLUTTER_VERSION }}-v2

      - name: Setup Rust
        if: steps.signing.outputs.enabled == 'true'
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: ${{ env.RUST_VERSION }}

      - name: Select Xcode 26
        if: steps.signing.outputs.enabled == 'true'
        run: |
          set -euo pipefail
          if [ -d /Applications/Xcode_26.3.app ]; then
            export DEVELOPER_DIR=/Applications/Xcode_26.3.app/Contents/Developer
          elif [ -d /Applications/Xcode_26.2.app ]; then
            export DEVELOPER_DIR=/Applications/Xcode_26.2.app/Contents/Developer
          elif [ -d /Applications/Xcode_26.1.app ]; then
            export DEVELOPER_DIR=/Applications/Xcode_26.1.app/Contents/Developer
          elif [ -d /Applications/Xcode_26.app ]; then
            export DEVELOPER_DIR=/Applications/Xcode_26.app/Contents/Developer
          else
            echo "Xcode 26 is required for App Store Connect iOS uploads." >&2
            ls -1 /Applications | grep '^Xcode' >&2 || true
            exit 1
          fi
          echo "DEVELOPER_DIR=$DEVELOPER_DIR" >> "$GITHUB_ENV"
          xcodebuild -version

      - name: Install CocoaPods
        if: steps.signing.outputs.enabled == 'true'
        run: gem install cocoapods -v ${{ env.COCOAPODS_VERSION }}

      - name: Import iOS signing certificate
        if: steps.signing.outputs.enabled == 'true'
        env:
          CERT_P12_B64: ${{ secrets.IOS_SIGNING_CERT_P12_B64 }}
          CERT_PASSWORD: ${{ secrets.IOS_SIGNING_CERT_PASSWORD }}
        run: |
          CERT_PATH="$RUNNER_TEMP/ios-signing.p12"
          printf '%s' "$CERT_P12_B64" | base64 -d > "$CERT_PATH"
          CERT_PASSWORD="$(printf '%s' "$CERT_PASSWORD" | tr -d '\r\n')"
          if ! openssl pkcs12 -in "$CERT_PATH" -passin pass:"$CERT_PASSWORD" -nokeys -clcerts -out /dev/null >/dev/null 2>&1; then
            echo "::error::Unable to decrypt iOS signing certificate. Recheck IOS_SIGNING_CERT_P12_B64 and IOS_SIGNING_CERT_PASSWORD are from the same .p12 export."
            exit 1
          fi
          KEYCHAIN="$RUNNER_TEMP/ios-signing.keychain-db"
          KEYCHAIN_PASSWORD="$(uuidgen)"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security set-keychain-settings -t 3600 -u "$KEYCHAIN"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          security import "$CERT_PATH" -k "$KEYCHAIN" -P "$CERT_PASSWORD" -T /usr/bin/codesign -T /usr/bin/security
          security list-keychains -s "$KEYCHAIN"
          security default-keychain -s "$KEYCHAIN"
          security set-key-partition-list -S apple-tool:,apple: -s -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN"
          echo "IOS_SIGN_KEYCHAIN=$KEYCHAIN" >> "$GITHUB_ENV"
          echo "IOS_SIGN_KEYCHAIN_PASSWORD=$KEYCHAIN_PASSWORD" >> "$GITHUB_ENV"

      - name: Install provisioning profile
        if: steps.signing.outputs.enabled == 'true'
        env:
          PROFILE_B64: ${{ secrets.IOS_PROVISIONING_PROFILE_B64 }}
        run: |
          PROFILE_PATH="$RUNNER_TEMP/profile.mobileprovision"
          printf '%s' "$PROFILE_B64" | base64 -d > "$PROFILE_PATH"
          PROFILE_PLIST="$RUNNER_TEMP/profile.plist"
          security cms -D -i "$PROFILE_PATH" > "$PROFILE_PLIST"
          PROFILE_UUID=$(/usr/libexec/PlistBuddy -c 'Print :UUID' "$PROFILE_PLIST")
          PROFILE_NAME=$(/usr/libexec/PlistBuddy -c 'Print :Name' "$PROFILE_PLIST")
          TEAM_ID=$(/usr/libexec/PlistBuddy -c 'Print :TeamIdentifier:0' "$PROFILE_PLIST")
          mkdir -p "$HOME/Library/MobileDevice/Provisioning Profiles"
          cp "$PROFILE_PATH" "$HOME/Library/MobileDevice/Provisioning Profiles/$PROFILE_UUID.mobileprovision"
          echo "IOS_PROVISIONING_PROFILE_SPECIFIER=$PROFILE_NAME" >> "$GITHUB_ENV"
          echo "IOS_TEAM_ID=$TEAM_ID" >> "$GITHUB_ENV"
          echo "IOS_CODE_SIGN_IDENTITY=Apple Distribution" >> "$GITHUB_ENV"

      - name: Install ExportOptions.plist
        if: steps.signing.outputs.enabled == 'true'
        env:
          EXPORT_OPTIONS_PLIST_B64: ${{ secrets.IOS_EXPORT_OPTIONS_PLIST_B64 }}
        run: |
          printf '%s' "$EXPORT_OPTIONS_PLIST_B64" | base64 -d > app/ios/ExportOptions.plist

      - name: Build signed iOS IPA
        if: steps.signing.outputs.enabled == 'true'
        env:
          GITHUB_API_PUBLIC_READONLY_TOKEN: ${{ github.token }}
          VERSION_BUILD_NUMBER: ${{ github.run_number }}
        run: |
          chmod +x scripts/build-ios.sh
          REPRODUCIBLE=0 ./scripts/build-ios.sh true

      - name: Upload signed IPA
        if: steps.signing.outputs.enabled == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: ios-ipa-signed
          path: |
            dist/ios/Stashi-Wallet-ios.ipa
            dist/ios/Stashi-Wallet-ios.ipa.sha256
          if-no-files-found: error

      - name: Upload IPA to TestFlight
        if: steps.signing.outputs.enabled == 'true' && steps.testflight.outputs.enabled == 'true'
        env:
          APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          APP_STORE_CONNECT_API_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_P8_B64: ${{ secrets.APP_STORE_CONNECT_API_KEY_P8_B64 }}
        run: |
          set -euo pipefail
          IPA_PATH="dist/ios/Stashi-Wallet-ios.ipa"
          if [ ! -f "$IPA_PATH" ]; then
            echo "Signed IPA not found at $IPA_PATH" >&2
            exit 1
          fi

          KEY_DIR="$HOME/.appstoreconnect/private_keys"
          mkdir -p "$KEY_DIR"
          KEY_PATH="$KEY_DIR/AuthKey_${APP_STORE_CONNECT_API_KEY_ID}.p8"
          printf '%s' "$APP_STORE_CONNECT_API_KEY_P8_B64" | base64 -d > "$KEY_PATH"
          chmod 600 "$KEY_PATH"

          xcrun altool --upload-app \
            --file "$IPA_PATH" \
            --type ios \
            --apiKey "$APP_STORE_CONNECT_API_KEY_ID" \
            --apiIssuer "$APP_STORE_CONNECT_API_ISSUER_ID"

  virustotal-scan:
    name: Security - VirusTotal Scan (Artifacts)
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs:
      - package-android
      - package-ios
      - package-windows
      - package-macos
      - package-linux
      - sign-android
      - sign-windows
      - sign-macos
      - sign-ios
    steps:
      - name: Check VirusTotal API key
        id: vt
        run: |
          if [ -n "${{ secrets.VIRUSTOTAL_API_KEY }}" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "::warning::Missing VIRUSTOTAL_API_KEY secret. Skipping VirusTotal artifact scanning." >&2
          fi

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        if: steps.vt.outputs.enabled == 'true'

      - name: Download artifacts
        if: steps.vt.outputs.enabled == 'true'
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          path: vt-artifacts

      - name: Scan artifacts with VirusTotal
        if: steps.vt.outputs.enabled == 'true'
        env:
          VIRUSTOTAL_API_KEY: ${{ secrets.VIRUSTOTAL_API_KEY }}
          VT_MIN_INTERVAL_SECONDS: "16"
          VT_POLL_TIMEOUT_SECONDS: "1200"
        run: |
          python3 scripts/virustotal_scan.py vt-artifacts vt-report
          {
            echo "## VirusTotal"
            echo ""
            cat vt-report/virustotal-results.md
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Upload VirusTotal report
        if: steps.vt.outputs.enabled == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: virustotal-report
          path: vt-report/*
          if-no-files-found: warn

  release:
    name: Release - Publish Artifacts
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    needs:
      - ci-success
      - backend-release-plan
      - package-cli
      - package-qortal-jni
      - package-native-ffi
      - package-ios-sdk
      - package-android-sdk
      - package-react-native-plugin
      - sign-android
      - sign-windows
      - sign-macos
      - sign-ios
      - virustotal-scan
    steps:
      - name: Checkout tag for release notes
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0

      - name: Download artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          path: release-artifacts

      - name: Collect release files
        env:
          CLI_CHANGED: ${{ needs.backend-release-plan.outputs.cli_changed }}
          QORTAL_CLI_CHANGED: ${{ needs.backend-release-plan.outputs.qortal_cli_changed }}
          QORTAL_JNI_CHANGED: ${{ needs.backend-release-plan.outputs.qortal_jni_changed }}
          NATIVE_FFI_CHANGED: ${{ needs.backend-release-plan.outputs.native_ffi_changed }}
          IOS_SDK_CHANGED: ${{ needs.backend-release-plan.outputs.ios_sdk_changed }}
          ANDROID_SDK_CHANGED: ${{ needs.backend-release-plan.outputs.android_sdk_changed }}
          REACT_NATIVE_PLUGIN_CHANGED: ${{ needs.backend-release-plan.outputs.react_native_plugin_changed }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          bash scripts/collect-github-release-assets.sh release-artifacts release

      - name: Import Stashi Wallet release signing key
        env:
          RELEASE_GPG_PRIVATE_KEY_B64: ${{ secrets.LINUX_GPG_PRIVATE_KEY_B64 }}
        run: |
          set -euo pipefail
          expected_primary="E4FB2399AECCF9B9447DED472CE65343401553A6"
          if [ -z "$RELEASE_GPG_PRIVATE_KEY_B64" ]; then
            echo "LINUX_GPG_PRIVATE_KEY_B64 must contain the Stashi Wallet release key." >&2
            exit 1
          fi

          printf '%s' "$RELEASE_GPG_PRIVATE_KEY_B64" | base64 -d > "$RUNNER_TEMP/release-signing-key.gpg"
          gpg --batch --import "$RUNNER_TEMP/release-signing-key.gpg"
          gpg --batch --import release-signing/public_key.asc
          rm -f "$RUNNER_TEMP/release-signing-key.gpg"

          mapfile -t public_fingerprints < <(
            gpg --batch --with-colons --show-keys --fingerprint release-signing/public_key.asc |
              awk -F: '$1 == "fpr" { print $10 }'
          )
          mapfile -t secret_fingerprints < <(
            gpg --batch --with-colons --list-secret-keys --fingerprint "$expected_primary" |
              awk -F: '$1 == "fpr" { print $10 }'
          )
          if [[ " ${public_fingerprints[*]} " != *" $expected_primary "* ]]; then
            echo "The repository public key does not match the Stashi Wallet release key." >&2
            exit 1
          fi
          if [[ " ${secret_fingerprints[*]} " != *" $expected_primary "* ]]; then
            echo "The configured private key cannot sign as the published Pirate release key." >&2
            exit 1
          fi

      - name: Create Treasure Chest compatible signature bundle
        env:
          GPG_PASSPHRASE: ${{ secrets.LINUX_GPG_PASSPHRASE }}
          GPG_SIGNING_KEY: E4FB2399AECCF9B9447DED472CE65343401553A6
        run: |
          set -euo pipefail
          chmod +x scripts/create-release-signature-bundle.sh
          scripts/create-release-signature-bundle.sh \
            release \
            "release/signatures-${GITHUB_REF_NAME}.zip" \
            "$GITHUB_REF_NAME" \
            "$GPG_SIGNING_KEY"

      - name: Build release notes from annotated tag
        run: |
          set -euo pipefail
          TAG_NAME="${GITHUB_REF_NAME}"
          TAG_REF="refs/tags/$TAG_NAME"
          TAG_TYPE="$(git cat-file -t "$TAG_REF" 2>/dev/null || true)"
          if [ "$TAG_TYPE" = "tag" ]; then
            TAG_BODY="$(git cat-file -p "$TAG_REF" | sed -n '1,/^$/d')"
          else
            echo "Tag '$TAG_NAME' is lightweight; using commit message fallback."
            COMMIT_SHA="$(git rev-parse "$TAG_REF" 2>/dev/null || true)"
            if [ -n "$COMMIT_SHA" ]; then
              COMMIT_SUBJECT="$(git log -1 --format=%s "$COMMIT_SHA")"
              COMMIT_BODY="$(git log -1 --format=%b "$COMMIT_SHA")"
              if [ -n "$COMMIT_BODY" ]; then
                TAG_BODY="$(printf "%s\n\n%s" "$COMMIT_SUBJECT" "$COMMIT_BODY")"
              else
                TAG_BODY="$COMMIT_SUBJECT"
              fi
            else
              TAG_BODY="$TAG_NAME"
            fi
          fi
          mkdir -p release-notes
          printf "%s\n" "$TAG_BODY" > release-notes/RELEASE_NOTES.md

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF_NAME}"
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release edit "$TAG" --notes-file release-notes/RELEASE_NOTES.md
          else
            gh release create "$TAG" --notes-file release-notes/RELEASE_NOTES.md
          fi
          gh release upload "$TAG" release/* --clobber

  publish-react-native-npm:
    name: Publish - React Native Plugin (npm)
    runs-on: ubuntu-latest
    if: >-
      github.event_name == 'push' &&
      startsWith(github.ref, 'refs/tags/v') &&
      needs.backend-release-plan.outputs.react_native_plugin_changed == 'true' &&
      vars.NPM_PUBLISH_ENABLED == 'true'
    environment: npm-publish
    permissions:
      contents: read
      id-token: write
    needs:
      - release
      - backend-release-plan
    steps:
      - name: Checkout publication verifier
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Download React Native npm package
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: react-native-pirate-wallet
          path: npm-artifact

      - name: Setup Node for trusted publishing
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: ${{ env.NODE_VERSION }}
          registry-url: https://registry.npmjs.org
          package-manager-cache: false

      - name: Install pinned npm
        run: |
          npm install --global --ignore-scripts npm@${{ env.NPM_VERSION }}
          test "$(npm --version)" = "${{ env.NPM_VERSION }}"

      - name: Verify npm packages
        env:
          EXPECTED_VERSION: ${{ needs.backend-release-plan.outputs.react_native_plugin_version }}
          MAX_NPM_TARBALL_BYTES: "195000000"
        run: |
          set -euo pipefail
          mapfile -t npm_tarballs < <(find npm-artifact -type f -name '*.tgz' -print)
          if [ "${#npm_tarballs[@]}" -ne 6 ]; then
            echo "Expected six npm tarballs, found ${#npm_tarballs[@]}" >&2
            exit 1
          fi

          verify_package() {
            local package_name="$1"
            local env_prefix="$2"
            local expected_tarball_name="$package_name-$EXPECTED_VERSION.tgz"
            local matches=()
            mapfile -t matches < <(
              find npm-artifact -type f -name "$expected_tarball_name" -print
            )
            if [ "${#matches[@]}" -ne 1 ]; then
              echo "Expected one $expected_tarball_name, found ${#matches[@]}" >&2
              exit 1
            fi

            local npm_tarball
            npm_tarball="$(realpath -- "${matches[0]}")"
            local checksum_file="$npm_tarball.sha256"
            if [ ! -f "$checksum_file" ]; then
              echo "Missing npm tarball checksum: $checksum_file" >&2
              exit 1
            fi
            (
              cd "$(dirname "$npm_tarball")"
              sha256sum --check "$(basename "$checksum_file")"
            )

            local tarball_size
            tarball_size="$(wc -c < "$npm_tarball" | tr -d '[:space:]')"
            if (( tarball_size > MAX_NPM_TARBALL_BYTES )); then
              echo "$package_name tarball exceeds the npm publish limit: $tarball_size bytes" >&2
              exit 1
            fi

            local metadata_dir
            metadata_dir="$(mktemp -d)"
            tar -xzf "$npm_tarball" -C "$metadata_dir" package/package.json
            local package_json="$metadata_dir/package/package.json"
            local actual_name
            local actual_version
            actual_name="$(node -e 'process.stdout.write(require(process.argv[1]).name)' "$package_json")"
            actual_version="$(node -e 'process.stdout.write(require(process.argv[1]).version)' "$package_json")"
            rm -rf "$metadata_dir"
            if [ "$actual_name" != "$package_name" ]; then
              echo "Unexpected npm package name: $actual_name" >&2
              exit 1
            fi
            if [ "$actual_version" != "$EXPECTED_VERSION" ]; then
              echo "$package_name version mismatch: expected $EXPECTED_VERSION, got $actual_version" >&2
              exit 1
            fi

            local local_integrity
            local_integrity="$(node -e '
              const crypto = require("crypto");
              const fs = require("fs");
              const digest = crypto.createHash("sha512").update(fs.readFileSync(process.argv[1])).digest("base64");
              process.stdout.write(`sha512-${digest}`);
            ' "$npm_tarball")"
            local view_json="/tmp/npm-view-$env_prefix.json"
            local view_error="/tmp/npm-view-$env_prefix.err"
            if npm view "$package_name@$EXPECTED_VERSION" dist.integrity --json \
                >"$view_json" 2>"$view_error"; then
              local registry_integrity
              registry_integrity="$(
                node scripts/read-npm-view-integrity.js "$view_json"
              )"
              if [ "$registry_integrity" != "$local_integrity" ]; then
                echo "$package_name@$EXPECTED_VERSION already exists with different contents" >&2
                exit 1
              fi
              echo "$package_name@$EXPECTED_VERSION is already published with the expected contents"
              echo "${env_prefix}_ALREADY_PUBLISHED=true" >> "$GITHUB_ENV"
            elif grep -q 'E404' "$view_json" "$view_error"; then
              echo "${env_prefix}_ALREADY_PUBLISHED=false" >> "$GITHUB_ENV"
            else
              cat "$view_json" "$view_error" >&2
              echo "Could not confirm that $package_name@$EXPECTED_VERSION is unpublished" >&2
              exit 1
            fi

            echo "${env_prefix}_TARBALL=$npm_tarball" >> "$GITHUB_ENV"
          }

          verify_package react-native-pirate-wallet-android NPM_ANDROID
          verify_package react-native-pirate-wallet-android-x86_64 NPM_ANDROID_X86_64
          verify_package react-native-pirate-wallet-ios-device NPM_IOS_DEVICE
          verify_package react-native-pirate-wallet-ios-simulator-arm64 NPM_IOS_SIMULATOR_ARM64
          verify_package react-native-pirate-wallet-ios-simulator-x86_64 NPM_IOS_SIMULATOR_X86_64
          verify_package react-native-pirate-wallet NPM_MAIN

      - name: Publish Android ARM npm package with provenance
        if: env.NPM_ANDROID_ALREADY_PUBLISHED != 'true'
        run: npm publish "$NPM_ANDROID_TARBALL" --access public

      - name: Publish Android x86_64 npm package with provenance
        if: env.NPM_ANDROID_X86_64_ALREADY_PUBLISHED != 'true'
        run: npm publish "$NPM_ANDROID_X86_64_TARBALL" --access public

      - name: Publish iOS device npm package with provenance
        if: env.NPM_IOS_DEVICE_ALREADY_PUBLISHED != 'true'
        run: npm publish "$NPM_IOS_DEVICE_TARBALL" --access public

      - name: Publish iOS arm64 simulator npm package with provenance
        if: env.NPM_IOS_SIMULATOR_ARM64_ALREADY_PUBLISHED != 'true'
        run: npm publish "$NPM_IOS_SIMULATOR_ARM64_TARBALL" --access public

      - name: Publish iOS x86_64 simulator npm package with provenance
        if: env.NPM_IOS_SIMULATOR_X86_64_ALREADY_PUBLISHED != 'true'
        run: npm publish "$NPM_IOS_SIMULATOR_X86_64_TARBALL" --access public

      - name: Publish React Native npm package with provenance
        if: env.NPM_MAIN_ALREADY_PUBLISHED != 'true'
        run: npm publish "$NPM_MAIN_TARBALL" --access public

  ci-success:
    name: CI - All Checks Passed
    runs-on: ubuntu-latest
    if: ${{ always() }}
    needs:
      - workflow-action-pinning
      - frb-codegen
      - rust-lint
      - rust-test
      - rust-qortal-tests
      - rust-property-tests
      - rust-migration-tests
      - rust-perf-tests
      - security-audit
      - security-deny
      - security-semgrep-rust
      # security-semgrep-dart disabled due to CI memory constraints
      - flutter-analyze
      - flutter-test
      - build-matrix
      - backend-release-plan
      - package-cli
      - package-qortal-jni
      - package-native-ffi
      - package-ios-sdk
      - package-android-sdk
      - package-react-native-plugin
      - verify-react-native-plugin-ios-example
      - package-android
      - package-ios
      - package-windows
      - package-macos
      - package-linux
    steps:
      - name: Verify required CI outcomes
        env:
          EVENT_NAME: ${{ github.event_name }}
          NEEDS_JSON: ${{ toJSON(needs) }}
        run: |
          python3 - <<'PY'
          import json
          import os

          needs = json.loads(os.environ["NEEDS_JSON"])
          allowed_skips = {
              "package-android",
              "package-ios",
              "package-windows",
              "package-macos",
              "package-linux",
          } if os.environ["EVENT_NAME"] == "pull_request" else set()

          invalid = []
          for job, details in sorted(needs.items()):
              result = details["result"]
              if result == "success":
                  continue
              if result == "skipped" and job in allowed_skips:
                  continue
              invalid.append(f"{job}={result}")

          if invalid:
              print("Required CI outcomes did not pass:")
              for outcome in invalid:
                  print(f"- {outcome}")
              raise SystemExit(1)

          print("All required CI checks passed successfully.")
          PY
